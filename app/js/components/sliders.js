(function () {

    const setings = {
        infinite: true,
        dots:true,
        arrows:false,
        slidesToShow: 1,
        slidesToScroll: 1,
        autoplay: true
    };

    $('.slider-pizza').slick(setings);
    $('.slider-burgers').slick(setings);
    $('.slider-salads').slick(setings);
    $('.slider-tacos').slick(setings);
    $('.slider-wraps').slick(setings);
    $('.slider-fries').slick(setings);
    $('.slider-drinks').slick(setings);

})()



